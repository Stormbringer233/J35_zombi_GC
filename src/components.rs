use crate::prelude::*;

#[derive(Debug)]
pub struct Zombi;

pub struct WorldMapTexture;
#[derive(Debug)]
pub struct Render {
    pub texture : Texture2D,
    pub color : Color,
    pub to_draw : bool,
}

pub struct Plot;
#[derive(Debug)]
pub struct CityPointer;
